# Omniphi Validator Orchestrator - Environment Configuration
# Copy this file to .env and update with your values

# =============================================================================
# API Settings
# =============================================================================
API_V1_STR=/api/v1
PROJECT_NAME=Omniphi Validator Orchestrator
VERSION=1.0.0
DEBUG=false

# =============================================================================
# Database Configuration
# =============================================================================
# Option 1: Use DATABASE_URL (overrides individual PostgreSQL settings)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# DATABASE_URL=sqlite:///./validator_orchestrator.db  # For development only

# Option 2: Individual PostgreSQL settings
POSTGRES_USER=omniphi
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=validator_orchestrator

# =============================================================================
# Security Configuration
# =============================================================================
# IMPORTANT: Generate a strong random secret key for production
# Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-in-production-REQUIRED

# JWT Settings
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Master API Key for admin operations
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
MASTER_API_KEY=your-master-api-key-change-in-production-REQUIRED

# =============================================================================
# Rate Limiting
# =============================================================================
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# =============================================================================
# CORS (Cross-Origin Resource Sharing)
# =============================================================================
# List of allowed origins (Python list format)
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:5173","https://validators.omniphi.xyz"]

# =============================================================================
# Omniphi Chain Configuration
# =============================================================================
# Chain ID for the Omniphi network
OMNIPHI_CHAIN_ID=omniphi-mainnet-1

# RPC endpoint for chain queries
OMNIPHI_RPC_URL=http://localhost:26657

# REST API endpoint
OMNIPHI_REST_URL=http://localhost:1317

# gRPC endpoint
OMNIPHI_GRPC_URL=localhost:9090

# Binary download URL (for provisioning)
OMNIPHI_BINARY_URL=https://github.com/omniphi/releases/download/v1.0.0/posd

# Genesis file URL
OMNIPHI_GENESIS_URL=https://raw.githubusercontent.com/omniphi/networks/main/mainnet/genesis.json

# =============================================================================
# Docker Configuration
# =============================================================================
# Docker network for validator containers
DOCKER_NETWORK=omniphi-validator-network

# Docker image for validator nodes
DOCKER_IMAGE=omniphi/validator-node:latest

# =============================================================================
# Cloud Provider Credentials (Optional)
# =============================================================================
# AWS Configuration
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key

# GCP Configuration
# GCP_PROJECT_ID=your-gcp-project
# GCP_CREDENTIALS_PATH=/path/to/credentials.json

# =============================================================================
# Production Deployment Notes
# =============================================================================
#
# 1. Security Checklist:
#    - Change SECRET_KEY to a random 32+ character string
#    - Change MASTER_API_KEY to a random 64 character hex string
#    - Use strong PostgreSQL password
#    - Set DEBUG=false
#    - Configure CORS to allow only your frontend domain
#    - Enable rate limiting (RATE_LIMIT_ENABLED=true)
#
# 2. Database:
#    - Use PostgreSQL in production (not SQLite)
#    - Enable SSL/TLS for database connections
#    - Set up regular backups
#
# 3. Monitoring:
#    - Set up application monitoring (Sentry, DataDog, etc.)
#    - Configure log aggregation
#    - Set up uptime monitoring
