[package]
name = "omniphi-contract-sdk"
version = "0.1.0"
edition = "2021"
description = "SDK for building Omniphi Intent Contracts"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
serde_json = "1"
